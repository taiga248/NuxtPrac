version: 2.1
executors:
  frontend-executor:
    docker:
      - image: circleci/node:10.15.3

workflows:
  version: 2
  all-check:
    jobs:
      - echo-hello:
          # filters:
          #   branches:
          #     only:
          #       - develop

jobs:
  echo-hello:
    executor: frontend-executor
    steps:
      - checkout
      - run: npm run echo
